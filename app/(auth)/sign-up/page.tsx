import AuthForm from "@/components/AuthForm";
import React from "react";

const page = () => {
  return (
    <div>
      {/* sign up */}
      <AuthForm type="sign-up" />
    </div>
  );
};

export default page;
